/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      './index.html',
      './src/**/*.{js,ts,jsx,tsx}', // include all your source files
    ],
    theme: {
      extend: {
        // Add custom theme extensions here, e.g., colors, spacing, fonts
      },
    },
    plugins: [],
  }